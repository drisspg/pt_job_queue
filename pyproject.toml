[project]
name = "ptq"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["typer>=0.15.0", "rich>=13.0.0"]

[project.scripts]
ptq = "ptq.cli:app"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
